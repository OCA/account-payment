<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>
       <record model='ir.actions.act_window' id='payment_order_method_action'>
            <field name='name'>Payment Order</field>
            <field name='res_model'>payment.order</field>
            <field name='view_type'>form</field>
            <field name='view_mode'>tree,form</field>
        </record>

        <menuitem action="payment_order_method_action" id='payment_order_menu' name="Payment Order" 
        	parent="account.menu_finance_entries" sequence="10"/>
        	
      	<record id="view_payment_order_tree" model="ir.ui.view">
            <field name="name">payment.order.tree</field>
            <field name="model">payment.order</field>
            <field name="arch" type="xml">
                <tree colors="blue:state == 'draft';gray:state in ('cancel','done');black:state == 'open'" string="Payment Order">
                    <field name="reference"/>
                    <field name="mode"/>
                    <field name="user_id"/>
                    <field name="company_id" groups="base.group_multi_company"/>
                    <field name="create_date"/>
                    <field name="date_done"/>
                    <field name="total"/>
                    <field name="state"/>
                </tree>
            </field>
        </record>
                    
        <record id="view_payment_order_form" model="ir.ui.view">
            <field name="name">payment.order.form</field>
            <field name="model">payment.order</field>
            <field name="arch" type="xml">
                <form string="Payment Order">
                <header>
                    <button name="set_to_confirmed" states="draft" string="Confirm Payments" type="object" class="oe_highlight"/>
                    <button name="launch_wizard" states="open" string="Make Payments" type="object" class="oe_highlight"/>
                    <button name="set_to_draft" states="cancel" string="Set to draft" type="object"/>
                    <button name="cancel" states="draft,open" string="Cancel Payments"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,open"/>
                </header>
                <sheet string="Payment Order">
                    <div class="oe_title">
                        <label for="reference" class="oe_edit_only"/>
                        <h1><field name="reference"/></h1>
                    </div>
                    <div class=" oe_right oe_button_box">
                        <button class="oe_inline oe_stat_button oe_right" name="%(action_payment_order_create)d" string="Invoices" Help="Select Invoices to Pay"
                               type="action" attrs="{'invisible':[('state', '!=', 'draft')]}" icon="fa-pencil-square-o" widget="statinfo"/>
                    </div>
                    <group>
                        <group>
                            <field name="user_id" context="{'default_groups_ref': ['base.group_user', 'base.group_partner_manager', 'account.group_account_invoice']}"/>
                            <field name="mode"/>
                            <field name="mode_type" invisible="1"/>
                        </group>
                        <group>
                            <field name="date_prefered"/>
                            <field name="date_scheduled" attrs="{'readonly':[('date_prefered','!=','fixed')]}"/>
                            <field name="company_id" widget='selection' groups="base.group_multi_company"/>
                        </group>
                    </group>
                    <field name="line_ids" context="{'order_id': active_id or False}" />                                                              
                    </sheet>
                </form>
            </field>
        </record> 
        
        
    </data>
</openerp>
