This module introduces a feature that allows payments to be configured to either remain in a `Draft` state or follow the standard process
and be marked as `Paid` immediately upon registration.

By enabling the draft state option, this module ensures that payments can undergo an `approval` or `recheck` process
before being finalized. This adds an additional layer of control and accuracy to the payment workflow.
